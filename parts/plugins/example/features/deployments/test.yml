---
apiVersion: batch/v1
kind: Job
metadata:
  name: test-deployment
spec:
  template:
    metadata:
      labels:
        app: test
    spec:
      containers:
        - name: test
          image: mongo
      restartPolicy: Never
